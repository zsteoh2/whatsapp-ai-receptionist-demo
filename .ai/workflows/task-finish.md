# Task Finish

1. Synchronize only authoritative documents whose facts changed.
2. Promote only verified, stable, reusable project-wide knowledge.
3. Update project memory and task `handoff.md`.
4. Run `.ai/scripts/validate-context.py`.
5. Set task status to `completed`, move its directory to `.ai/tasks/completed/`, and clear `.ai/runtime/active-task.json`.
6. Report completion, verification, documentation updates, and the next step or blocker.
