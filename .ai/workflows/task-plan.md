# Task Plan

1. Set stage to `planning`.
2. Fill goal, scope, out-of-scope, constraints, affected modules, ordered steps, risks, and verification method in `plan.md`.
3. Check referenced requirements, business rules, architecture, and accepted ADRs.
4. Resolve unknown assumptions or mark them explicitly.
5. When the minimal plan is accepted, set stage to `approved`, then continue with `task-implement.md`.
