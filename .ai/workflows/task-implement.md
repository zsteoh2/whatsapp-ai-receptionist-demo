# Task Implement

1. Set stage to `implementation`.
2. Follow the approved `plan.md` and preserve unrelated user changes.
3. Record only meaningful progress and discoveries in `implementation-log.md`.
4. If an assumption fails, stop using it, update the plan, record the change, and continue from the revised plan.
5. Run focused checks during implementation.
6. When implementation is ready, continue with `task-check.md` using an independent agent or separate review pass when possible.
